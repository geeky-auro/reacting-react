export const content = ['./src/**/*.{html,js,jsx,ts,tsx}'];
export const theme = {
  extend: {},
  darkMode: 'class', // Correct placement of darkMode within the theme object
};
export const plugins = [];
